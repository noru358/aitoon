# Production font

`NotoSansKR-Regular.woff2` is the project-default Korean lettering font. It is
distributed under the SIL Open Font License in `OFL.txt`.

Lettering plans bind the exact font SHA-256. The runtime converts WOFF2 to a
cache-local TTF for Pillow without changing the registered source bytes.

